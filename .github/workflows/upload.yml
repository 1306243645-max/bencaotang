name: Deploy Site
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install -g surge 2>/dev/null
      - run: surge --project . --domain miaoshoutang.surge.sh
